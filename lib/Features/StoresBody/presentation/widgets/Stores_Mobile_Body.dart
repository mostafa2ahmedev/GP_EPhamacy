import 'package:flutter/material.dart';

class StoresMobileBody extends StatelessWidget {
  const StoresMobileBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("StoresMobileBody");
  }
}
